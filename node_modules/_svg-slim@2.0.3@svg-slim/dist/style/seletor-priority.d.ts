import { ISelector } from 'svg-vdom';
import { ISeletorPriority } from '../../typings/style';
export declare const getSelectorPriority: (seletors: ISelector[]) => ISeletorPriority;
export declare const overrideAble: (priority1: ISeletorPriority, priority2: ISeletorPriority) => boolean;
