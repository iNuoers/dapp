import { IDocument } from 'svg-vdom';
import { IRuleOption } from '../../typings';
export declare const combinePath: (dom: IDocument, { option: { disregardFill, disregardOpacity, } }: IRuleOption) => Promise<void>;
