import { IRuleOption } from '../../typings';
import { IDom } from '../../typings/node';
export declare const rmIllegalStyle: (dom: IDom, { params: { rmAttrEqDefault, ignoreKnownCSS, }, browsers, }: IRuleOption) => Promise<void>;
