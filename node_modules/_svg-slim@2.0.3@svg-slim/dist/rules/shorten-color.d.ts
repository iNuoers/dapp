import { IRuleOption } from '../../typings';
import { IDom } from '../../typings/node';
export declare const shortenColor: (dom: IDom, { option: { rrggbbaa, }, params: { opacityDigit, }, }: IRuleOption) => Promise<void>;
