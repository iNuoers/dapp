import { IRuleOption } from '../../typings';
import { IDom } from '../../typings/node';
export declare const shortenStyleTag: (dom: IDom, { params: { ignoreKnownCSS, }, }: IRuleOption) => Promise<void>;
