import { IDocument } from 'svg-vdom';
import { IRuleOption } from '../../typings';
export declare const rmAttribute: (dom: IDocument, { option: { keepEvent, keepAria, }, params: { rmAttrEqDefault, }, }: IRuleOption) => Promise<void>;
