import { IDocument } from 'svg-vdom';
import { IRuleOption } from '../../typings';
export declare const combineTransform: (dom: IDocument, { params: { trifuncDigit, sizeDigit, angelDigit, } }: IRuleOption) => Promise<void>;
