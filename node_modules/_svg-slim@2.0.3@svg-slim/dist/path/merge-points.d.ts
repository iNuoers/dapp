import { IPathResultItem } from '../../typings';
export declare const doMerge: (threshold: number, vals: number[]) => number[];
export declare const mergePoints: (threshold: number, pathArr: IPathResultItem[]) => IPathResultItem[];
