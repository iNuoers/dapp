import { INode, IParentNode, ITextNode, TCheckFn } from '../../typings/node';
/**
 * @param condition 判断条件
 * @param node 起始节点（起始节点不参与判断）
 * @param breakImmediate 设置为 true 表示找到第一个符合条件的元素即中断
 */
export declare const getNodesByCondition: <T extends INode = IParentNode | ITextNode>(condition: TCheckFn, node: INode, breakImmediate?: boolean) => T[];
